{
    "_COMMENT" : " must be specified. units are seconds",
    "universallinkdelay" : "1", 

    "_COMMENT" : " must be specified, units are bytes per second.",
    "universallinkbitrate" : "1000",

    "_COMMENT" : " if not specified, defaults to /default/",
    "_COMMENT" : " modifies how tc is used to limit link bandwidth - ",
    "_COMMENT" : " /default/ limits the bandwidth based on the contact plan.",
    "_COMMENT" : " /none/ does not use tc to limit the bandwidth and simply allows ION to be the limiter.",
    "tc_bandwidth_limiting" : "default",

    "_COMMENT" : " if not specified, defaults to http",
    "_COMMENT" : " only applies if no GNS3 config file is found in a default location.",
    "gns3serverptcl" : "http", 

    "_COMMENT" : " if not specified, defaults to localhost",
    "_COMMENT" : " only applies if no GNS3 config file is found in a default location.",
    "gns3server" : "localhost",

    "_COMMENT" : " if not specified, defaults to 3080",
    "_COMMENT" : " only applies if no GNS3 config file is found in a default location.",
    "gns3serverport" : "3080", 

    "_COMMENT" : " if not specified, defaults to 'contacts.ionrc' and creates a default always up contactfile. ",
    "_COMMENT" : " if specified contact file is not found or otherwise cannot be opened, a default contact plan will be generated.",
    "contactfile" : "default",

    "_COMMENT" : " if not specified, defaults to 192.168",
    "net_address" : "192.168",

    "_COMMENT" : " if not specified, defaults to udp",
    "link_protocol" : "ltp",  

    "_COMMENT" : " if not specified, defaults to False",
    "debug" : "True", 

    "_COMMENT" : " if not specified, defaults to /root/ion_config/",
    "container_config_path" : "/root/ion_config/", 

    "_COMMENT" : " if not specified, defaults to 4556",
    "ion_port" : "4556"
}
